<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    {{#each movies}}
    <li class="list-group-item">
      <div class="lead">
        <a href="/movies/{{this._id}}">{{this.title}}, <small>{{this.title}}</small></a>
      </div>
    </li>
    {{/each}}
  </div>
</div>